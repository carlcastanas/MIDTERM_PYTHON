# 1.	Program that accepts a given distance and convert it to its corresponding KM value using the formula: km = distance*1000  

distance = float(input("Enter value in distance: "))
km = distance * 1000
print('%0.2f distance is distance to %0.2f kilometers' %(distance,km))
